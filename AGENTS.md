# 项目工作规则

## 通用规则

1. 下载软件、安装包或较大的外部文件前，先让用户指定存储位置。
2. 指定的项目目录里只放项目相关文件；过程中需要安装的组件或软件不要放进项目目录。
3. 系统组件装进 C 盘，通用软件装到 `D:\Program`。
4. 用户没有明确说“打包”，不要做正式 APK 交付。
5. 每次完成代码、资源、题库或配置改动后，默认执行直连更新到当前安卓模拟器；只有用户明确说“不更新”“先不更新”或“不用直连”时才跳过。
6. 不要回滚或删除用户已有改动；清理大批文件前必须先确认。

## 找不同生图硬标准

以后新增找不同关卡时，必须先按下面标准生成和验收，不需要用户重复提醒：

1. 每关素材必须是左右两张方图，最终项目文件为：
   - `assets/leisure/spot/ai/level_N_left.jpg`
   - `assets/leisure/spot/ai/level_N_right.jpg`
2. 推荐先生成一张横向 `2:1` 源图：左 50% 是左方图，右 50% 是右方图；切图后左右图都保存为 `1024x1024` JPG。
3. 如果模型生成的是整张方图、竖图、上下拼接图、带留白图，不能直接入库；必须重新生成或让用户手动保存合格图。
4. 每个新增关卡默认只设计 `5` 处细微差异，差异必须自然融入原图，例如物体缺失、纹理变化、反光变化、颜色轻微变化、位置轻微变化。
5. 禁止用贴纸、图标、彩色圆点、星星、爱心、外框、文字或任何明显标记来表示差异。
6. 左右图必须保持同一视角、同一构图、同一光照、同一主体布局；不能变成两张不同场景。
7. 风格优先轻写实、真实材质、儿童友好；可以换主题，但不能降低成简单渲染图或明显拼贴图。
8. 新增关卡接入代码时必须是未手动标记状态：
   - `manualMarked: false`
   - 先放 5 个临时坐标保持结构完整，后续由用户在 APP 标记模式里手动保存覆盖。
9. 已经由用户手动标记并固化的旧关卡，才允许设置为 `manualMarked: true`。
10. 入库前必须做本地验收：
    - 检查源图是 `2:1` 或确认可无损切成左右方图。
    - 检查输出左右图都是 `1024x1024`。
    - 生成预览图快速目检，确认没有切错、没有明显标记、没有文字。
    - 更新或新增测试，确认资源被引用、文件存在、每个新增关卡 5 个差异点、且新增关卡为 `manualMarked: false`。
11. 使用内置生图时，如果图片没有直接落盘，可以从 Codex 会话记录里的 `image_generation_end.result` base64 还原；如果仍无法还原，再让用户手动保存到本地后继续处理。
12. 处理图片脚本优先使用本机 Miniforge：`D:\Program\Miniforge3\python.exe`。如果缺少库，先说明原因再决定是否安装。
